"""`python -m samskriti` — the interactive demo, no download required.

Also installed as the `samskriti` console command, so after pip install the whole
test is one word.
"""
from .talk import main

main()
